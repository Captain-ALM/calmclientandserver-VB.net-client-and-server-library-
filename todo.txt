Add support for relays

Add proper return values from functions (Instead of booleans)

Merge MSG and packet classes (so packet can return bytes and consume bytes)

Mugfixes + Code Cleanup

Fix the system header security issue

Fix the encryption/decryption block using synclock